# src/

Placeholder source directory for the future MQL5 bridge implementation.

Empty on purpose — the bridge is **not implemented** during bootstrap
(the bootstrap milestone deliberately excludes the MQL5 event bridge).

Future layout (see `../docs/architecture.md`):

```text
src/
|-- Include/     shared MQL5 headers (future)
|-- Experts/     bridge expert (future)
+-- Scripts/     auxiliary scripts (future)
```