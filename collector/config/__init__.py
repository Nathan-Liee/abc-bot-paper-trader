"""Placeholder module boundary: configuration loading.

Future home of the loader for ``config/settings.template.yaml`` and
`.env` files. Only non-execution values (environment name, storage
paths, log level) may ever be loaded; safety flags stay hard-coded in
``collector.settings``.
"""

__all__: list[str] = []
