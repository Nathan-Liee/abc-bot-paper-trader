# Placeholder directory. This file exists to keep the layout in git.

# scripts/
# Operational helper scripts (e.g. validation, environment bootstrap)
# are planned here in later milestones. Bootstrap does not add scripts
# that execute business logic.